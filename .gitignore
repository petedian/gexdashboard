# Environment variables and credentials (NEVER COMMIT THESE!)
.env
tasty_token.json
streamer_token.json
streamer_token.txt

# Python
*.pyc
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info
dist/
build/
*.log

# Virtual environments
venv/
env/
ENV/
.venv

# Streamlit
.streamlit/

# Data files (exclude data, but keep documentation images)
historical data/
*.csv
*.xlsx
# Note: images/ folder is intentionally NOT excluded (documentation screenshots)

# Databases (runtime data, not source -- collector regenerates these)
*.db
*.db-journal
*.db-shm
*.db-wal

# Logs (operational/runtime, not source)
*.log
mes_pipeline_log.txt

# Caches (disk-backed, regenerated at runtime)
chart_cache/

# Personal/private notes -- not dashboard source, kept out of the public repo
knowledge_base/
TRADING_PLAN.md

# Claude Code local settings (machine-specific, not project config)
.claude/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Testing artifacts
.pytest_cache/
.coverage
htmlcov/

# OS files
Thumbs.db
desktop.ini
